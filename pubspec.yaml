name: firebase_template_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  #asssets
  cupertino_icons: ^1.0.2
  google_fonts: ^2.1.0 #https://pub.dev/packages/google_fonts
  #json
  json_annotation: ^4.4.0 #https://pub.dev/packages/json_annotation
  #url
  url_launcher: ^6.0.15 #https://pub.dev/packages/url_launc.her
  # 状態管理
  flutter_riverpod: ^1.0.3 #https://pub.dev/packages/riverpod
  #firebase
  firebase_core: ^1.13.1 #https://pub.dev/packages/firebase_core
  cloud_firestore: ^3.1.10 #https://pub.dev/packages/cloud_firestore
  firebase_auth: ^3.3.11 #https://pub.dev/packages/firebase_auth
  google_sign_in: ^5.2.1
  # firebase_crashlytics: ^2.2.3 #https://pub.dev/packages/firebase_crashlytics
  # version管理
  package_info_plus: ^1.3.0 #https://pub.dev/packages/package_info_plus
  version: ^2.0.0 #https://pub.dev/packages/version
  #widget
  flutter_progress_hud: ^2.0.0 #https://pub.dev/packages/flutter_progress_hud
  flutterfire_ui: ^0.3.1 #https://pub.dev/packages/flutterfire_ui
  # レスポンシブ
  auto_size_text: ^3.0.0
  sizer: ^2.0.15 # https://pub.dev/packages/sizer
  # 翻訳
  intl: ^0.17.0 #https://pub.dev/packages/intl/install
  #QR code
  qr_flutter: ^4.0.0 #https://pub.dev/packages/qr_flutter
  flutter_barcode_scanner: ^2.0.0 #https://pub.dev/packages/flutter_barcode_scanner
  #権限
  app_tracking_transparency: ^2.0.2 #https://pub.dev/packages/app_tracking_transparency/install


dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  # 静的解析
  flutter_lints: ^1.0.0
  dart_code_metrics: ^4.8.1 #https://pub.dev/packages/dart_code_metrics
  # freezed
  build_runner: any
  freezed: ^1.1.1 #https://pub.dev/packages/freezed
  json_serializable: any
  # test
  mockito: ^5.0.0 #https://pub.dev/packages/mockito
  firebase_auth_mocks: ^0.8.2
  fake_cloud_firestore: ^1.2.0


flutter:

  uses-material-design: true
